# 后端
*.exe
*.exe~
*.dll
*.so
*.dylib
go-ldap-admin
*.test
*.out
tmp
docs/docker-compose/data
data
.docker-data
bin
release
.worktrees
.idea
.vscode
.token

# 前端
web/node_modules/
web/dist/
.DS_Store
npm-debug.log*
**/*.log
.webpack

# 通用
dist

# 个人/本地操作脚本与笔记（不应进入仓库，可能泄露本地路径）
上传到GitHub*.md
重新上传到GitHub*.sh
提交并推送到GitHub*.sh
rewrite-git-author.sh

# 安全：RSA 私钥不入库（历史遗留 + 运行期自动生成），登录加解密密钥改为运行期注入/生成
config/*.pem
data/rsa_private.pem

# 安全：运行配置不入库（可能含真实密钥），改用 config.example.yml 作模板 + 环境变量注入。
# 首次运行请 `cp config.example.yml config.yml`（缺失时程序会自动回退到模板启动）
config.yml
